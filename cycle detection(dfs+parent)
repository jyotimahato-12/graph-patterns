bool hasCycle(int node, int parent, vector<vector<int>>& adj, vector<bool>& vis) {
    vis[node] = true;
    for (int nei : adj[node]) {
        if (!vis[nei]) {
            if (hasCycle(nei, node, adj, vis)) return true;
        } else if (nei != parent) return true;
    }
    return false;
}
