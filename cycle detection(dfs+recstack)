bool dfsCycle(int u, vector<vector<int>>& adj, vector<bool>& vis, vector<bool>& rec) {
    vis[u] = rec[u] = true;
    for (int v : adj[u]) {
        if (!vis[v] && dfsCycle(v, adj, vis, rec)) return true;
        if (rec[v]) return true;
    }
    rec[u] = false;
    return false;
}
