queue<int> q;
for (int src : sources) {
    q.push(src);
    dist[src] = 0;
}
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int v : adj[u]) {
        if (dist[v] > dist[u] + 1) {
            dist[v] = dist[u] + 1;
            q.push(v);
        }
    }
}
